(function () {
    'use strict';

    anchors.options = {
        visible: 'always',
    };

    document.addEventListener('DOMContentLoaded', function (event) {
        anchors.add('.anchor');
    });
})();
